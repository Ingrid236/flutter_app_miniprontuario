# Contracts

This project is a fully offline MVP. It does not integrate with any external REST APIs, webhooks, or third-party backend services.

All "contracts" in this project refer to the local interfaces between the Domain layer and the Data layer (e.g., `PatientRepository` and `ProcedureRepository`), which are defined directly in the Dart code.
