# Quickstart

## Getting Started

1. Install Flutter SDK.
2. Clone the repository and navigate to `C:\miniprontuario\flutter_app_miniprontuario`.
3. Run `flutter pub get` to install dependencies.
4. Run the app using `flutter run` on a connected device or emulator.

## Testing

Execute tests with:
```bash
flutter test
```
